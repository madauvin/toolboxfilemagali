---
title: "flat_get_one_file_size.Rmd empty"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r development, include=FALSE}
library(testthat)
```

<!--
 You need to run the 'description' chunk in the '0-dev_history.Rmd' file before continuing your code there.
-->

```{r development-load}
# Load already included functions if relevant
pkgload::load_all(export_all = FALSE)
```

# get_one_file_size

```{r function-get_one_file_size}
#' get_one_file_size Title
#'
#' @return 1
#' @export
#'
#' @examples
get_one_file_size <- function() {
  1
}
```

```{r examples-get_one_file_size}
get_one_file_size()
```

```{r tests-get_one_file_size}
test_that("get_one_file_size works", {

})
```


```{r development-inflate, eval=FALSE}
# Run but keep eval=FALSE to avoid infinite loop
# Execute in the console directly
fusen::inflate(flat_file = "dev/flat_get_one_file_size.Rmd", vignette_name = "Minimal")
```
