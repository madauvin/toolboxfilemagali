---
title: "search folder size"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{search-folder-size}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(toolboxfilemagali)
```

<!-- WARNING - This vignette is generated by {fusen} from dev/flat_search_folder_size.Rmd: do not edit by hand -->

# search_folder_size

```{r examples-search_folder_size}
library(dplyr)
library(purrr)
temp_dir <- tempdir()
file1 <- file.path(temp_dir, "file1.csv")
file2 <- file.path(temp_dir, "file2.csv")
write.csv(iris, file1, row.names = FALSE)
write.csv(mtcars, file2, row.names = FALSE)

# Utiliser la fonction search_folder_size avec un seuil de 1000 octets
result <- search_folder_size(temp_dir, 1000)
print(result)

# Supprimer les fichiers temporaires après utilisation
unlink(file1)
unlink(file2)
```

